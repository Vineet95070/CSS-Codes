#include <stdio.h>
int main(){
	
	char hexa[100];
	int len;
	printf("Enter the Hexadecimal Value : ");
	scanf("%s",  &hexa);
	
	len=strlen(hexa);
	
	printf("%d", len);
	
	
return 0;	
}
